anthropic>=0.40.0
langgraph>=0.2.50
langchain-anthropic>=0.2.0
pydantic>=2.5.0
pyyaml>=6.0
python-dotenv>=1.0.0
tenacity>=8.2.0

# Real parsers for the validator node (replaces LLM-based validation)
openapi-spec-validator>=0.7.1
gherkin-official>=29.0.0

# Frontend
streamlit>=1.40.0

# Tests (instalar con: pip install -r requirements.txt)
pytest>=8.0.0
pytest-asyncio>=0.23.0
